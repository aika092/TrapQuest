Cutoffs by Knickers begins here.

cutoffs is a kind of knickers. cutoffs is usually denim. cutoffs is usually totally-exclusive. The armour of cutoffs is 10. Understand "pair", "of" as cutoffs. The soak-limit of cutoffs is usually 12.

To compute SelfExamineDesc of (K - a cutoffs):
	say "A pair of tiny tiny denim cutoffs fails to provide much dignity to your [ShortDesc of hips]. ";

To decide which number is the initial outrage of (C - a cutoffs):
	decide on 4.

To say selfexamineuniquetitle of (K - a cutoffs):
	say "pair of cutoffs".

To decide which object is the potential-upgrade-target of (C - a cutoffs):
	let W be a random off-stage butt window cutoffs;
	if W is clothing and a random number between 1 and 3 > 1, decide on W;
	decide on a random off-stage hotpants.

Part 1 - Neat Cutoffs

neat cutoffs is a kind of cutoffs. There is 1 neat cutoffs. The printed name of neat cutoffs is usually "[TQlink of item described][item style][cumdesc]pair of [unless magic-curse of the item described is bland or curse-ID of the item described is unsure][magic-curse] [end if][raw-magic-modifier-desc]neat denim cutoffs[clothing-title-after][TQxlink of item described][verb-desc of item described]". The text-shortcut of neat cutoffs is "ndc".

To decide which figure-name is clothing-image of (C - a neat cutoffs):
	decide on figure of neat cutoffs.

To say ClothingDesc of (K - a neat cutoffs):
	say "Extremely tiny denim shorts, these are skimpier than most common underwear. They are so tight that they [if K is worn]are[otherwise]would be[end if] impossible to displace without completely removing them.".
Figure of neat cutoffs is the file "cutoffs1.png".

Part 2 - Torn Cutoffs

torn cutoffs is a kind of cutoffs. There is 1 torn cutoffs. The printed name of torn cutoffs is usually "[TQlink of item described][item style][cumdesc]pair of [unless magic-curse of the item described is bland or curse-ID of the item described is unsure][magic-curse] [end if][raw-magic-modifier-desc]torn denim cutoffs[clothing-title-after][TQxlink of item described][verb-desc of item described]". The text-shortcut of torn cutoffs is "tdc".

To decide which figure-name is clothing-image of (C - a torn cutoffs):
	decide on figure of torn cutoffs.

To say ClothingDesc of (K - a torn cutoffs):
	say "Extremely tiny denim shorts, these are skimpier than most common underwear. The waist is wide enough that wearing them [if K is worn]is[otherwise]would be[end if] pretty comfortable.".
Figure of torn cutoffs is the file "cutoffs2.png".

Definition: a torn cutoffs is displacable:
	decide yes.

Part 3 - Butt Window Cutoffs

butt window cutoffs is a kind of cutoffs. There is 1 unique butt window cutoffs. A butt window cutoffs is usually crotch-assless.

The printed name of butt window cutoffs is usually "[TQlink of item described][item style][cumdesc]pair of [unless magic-curse of the item described is bland or curse-ID of the item described is unsure][magic-curse] [end if][raw-magic-modifier-desc]butt window denim cutoffs[clothing-title-after][TQxlink of item described][verb-desc of item described]". The text-shortcut of butt window cutoffs is "bwc".

To decide which figure-name is clothing-image of (C - a butt window cutoffs):
	decide on figure of butt window cutoffs.

To decide which number is the initial outrage of (C - a butt window cutoffs):
	decide on 6.

To say ClothingDesc of (K - a butt window cutoffs):
	say "Denim shorts with a heart shaped hole at the back that exposes your [buttcheeks]. It keeps your buttcheeks naturally closed which means that [if K is worn]your[otherwise]the wearer's[end if] asshole isn't constantly on show, but it still allows easy anal access when required.".
Figure of butt window cutoffs is the file "cutoffs3.png".

To decide which object is the potential-upgrade-target of (C - a butt window cutoffs):
	decide on a random off-stage g-string.

Cutoffs ends here.

